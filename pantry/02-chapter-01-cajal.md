# CAJAL Figure Intelligence — Chapter 1 (placeholder)

**Source:** `chapters/02-chapter-01.md`
**Mode:** `/scan silent`
**Domain note:** AI+1 practitioner guide / coding. At time of scan the chapter is a **placeholder skeleton**: title (`# Chapter 1`), TL;DR boilerplate ("This chapter gives a working overview of Chapter 1…"), and an HTML comment instructing the author to replace the placeholder and use `<!-- → [TYPE: description] -->` markers for figures. No prose body, no concept, no mechanisms, no author-placed `![...](images/...png)` figures.

---

## Density Recommendation

**0 figures, Minimal density (placeholder).** No content to architect. Figure budget will be set on re-scan once the chapter has a concept, a mechanism, and named trade-offs.

---

## Zone Map (deferred)

- **MC:** TBD on re-scan. For a coding-domain chapter 1, plausible mechanisms include: the AI-assisted edit loop (prompt → diff → review → commit), the model-context boundary, or the autocomplete-vs-architecture stratification. None of these are present yet.
- **VG:** TBD. Likely candidates if the chapter develops them: a tool-tier taxonomy (chat / inline complete / agentic), a context-window spatial diagram, or a responsibility-boundary block (who owns the diff).
- **PQ:** TBD. If the chapter cites quantitative claims (e.g., acceptance rates, time-to-merge deltas, lint-pass rates), a single bar chart may earn its place.

---

## Re-scan trigger

**Re-scan when the placeholder fills in.** Trigger figure budgeting when any of these emerge:

1. The chapter names a coding-specific mechanism with 3+ steps and a failure mode (warrants a process figure).
2. A taxonomy of AI coding tools or tasks with 4+ categories compared on shared axes (warrants a comparison block — but **drop UI screenshots / mockups**; those belong in picture research, not CAJAL).
3. Quantitative benchmark claims (warrants a chart; Okabe-Ito palette, no red-green encoding).
4. A worked example with code diffs (drop — code is typography / syntax-highlighted listing, not CAJAL).

---

## What CAJAL Declines to Architect

- **TL;DR boilerplate.** Templated text, no claims to render.
- **Author-intent HTML comment.** Not chapter content.
- **Any future UI mockups / IDE screenshots / chat transcripts.** Picture research / layout pass, not CAJAL.
- **Any future code listings.** Typography, not figure-shaped.

---

## Video Candidate Pass

**No figures, no video candidates.** Re-evaluate on next scan. If the chapter eventually develops the AI-assisted edit loop as a dynamic process, that may become a mild video candidate (cyclic-process criterion).
